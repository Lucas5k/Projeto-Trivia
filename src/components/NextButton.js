import React, { Component } from 'react';

class NextButton extends Component {
  render() {
    return (
      <button type="button">
        NextButton
      </button>
    );
  }
}

export default NextButton;
